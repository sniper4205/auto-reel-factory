function buildCharacterDNA(character) {
  return `
same person across all scenes,
same facial structure,
same eyes,
same nose,
same lips,
same skin tone,
consistent identity,
recognizable face,
no variation in identity
`;
}

module.exports = { buildCharacterDNA };
